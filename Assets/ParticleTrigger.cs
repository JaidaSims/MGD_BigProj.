using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ParticleTrigger : MonoBehaviour
{

    // ParticleSystem Fireworks;
    // bool FireworksTrig = Marble_Movement.GetFireworksBool = false;

    // Start is called before the first frame update
    void Start()
    {
      //  Fireworks = this.GetComponent<ParticleSystem>(); 
    }

    // void Update(){
    //     if(FireworksTrig(true)){
    //         Fireworks.Play();
    //     }
    // }
}
